pipeline {			
    agent any
    stages {			
        stage('Build') {			
            steps {			
                echo 'MASTER UPSTREAM executed'	
                echo $JOB_NAME 
            }			
        }			
    }			
}		
